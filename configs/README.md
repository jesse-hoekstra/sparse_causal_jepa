# configs/

Hydra configuration tree (Hydra is the config system — D1/D5: matches the vendored le-wm and
visreg repos). Will hold composable config groups for model, data, losses (regularizer selectable
`visreg` | `sigreg` per D3), training, and eval.

**Owner: experiment-infra-engineer.** No configs yet — this placeholder fixes ownership.