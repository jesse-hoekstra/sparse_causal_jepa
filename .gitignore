# --- Never committed: data, checkpoints, run outputs (see .claude/agents/README.md) ---
# Anchored to the repo root: the unanchored `data/` also matched
# src/scjepa/data/ at any depth, so the whole data MODULE was never committed
# (discovered 2026-07-19 when the Isambard clone came up without it).
/data/
checkpoints/
wandb/
# Hydra run dirs / sweep dirs (no inline comments — git treats them as pattern text)
outputs/
sources/
multirun/
*.ckpt
*.pt
*.pth

# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
*.egg
build/
dist/
.venv/
venv/
.python-version

# --- Tooling caches ---
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

# --- Editors / OS cruft ---
.DS_Store
.idea/
.vscode/
*.swp