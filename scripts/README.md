# scripts/

Entry points: `train.py`, `eval.py`, `prepare_data.py` (all Hydra-driven). Scripts stay thin —
logic lives in `src/scjepa/`.

**Owner: experiment-infra-engineer** (`prepare_data.py` jointly with data-pipeline-engineer).
No scripts yet — this placeholder fixes ownership.